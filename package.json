{
  "name": "further-engine-site",
  "version": "1.1.0",
  "private": true,
  "description": "Further Engine — official website (GitHub Pages)",
  "scripts": {
    "build": "tailwindcss -i src/input.css -o assets/css/app.css --minify",
    "dev": "tailwindcss -i src/input.css -o assets/css/app.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "3.4.17"
  }
}
